a random integer between min (included) and max (excluded):
`Math.floor(Math.random() * (max - min) ) + min;`

a random integer between min and max (both included):
`Math.floor(Math.random() * (max - min + 1) ) + min;`

